<script setup>
import {computed} from 'vue';
import {Link} from '@inertiajs/vue3';

const props = defineProps({
    href: String,
    active: Boolean,
});

const classes = computed(() => {
    return props.active
        // ? 'flex text-sm font-bold text-gray-900 hover:text-primary-700 dark:text-white dark:hover:text-primary-500'
        // : 'flex text-sm font-thin text-gray-900 hover:text-primary-700 dark:text-white dark:hover:text-primary-500'
        ? 'inline-flex items-center px-1 pt-1 border-b-2 border-primary-green-400 text-sm font-medium leading-5 text-gray-700 dark:text-white focus:outline-none focus:border-primary-green-200 transition duration-150 ease-in-out'
        : 'inline-flex items-center px-1 pt-1 border-b-2 border-transparent text-sm font-medium leading-5 text-gray-500 dark:text-gray-300 hover:text-gray-300 hover:border-gray-300 focus:outline-none focus:text-gray-700 focus:border-gray-300 transition duration-150 ease-in-out';
});
</script>

<template>
    <Link :class="classes" :href="href">
        <slot/>
    </Link>
</template>
